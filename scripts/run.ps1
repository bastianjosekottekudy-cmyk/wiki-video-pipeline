# Start (or restart) the app in this window.
# For background / Windows startup, use: .\scripts\restart-app.ps1 -Background
# Requires PowerShell 5.1+

$ErrorActionPreference = "Stop"
& "$PSScriptRoot\restart-app.ps1"
